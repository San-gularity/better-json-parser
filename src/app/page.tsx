import JsonParser from "@/Components/json_parser";
import Image from "next/image";

export default function Home() {
  return (
    
    <div className="w-full h-screen font-[family-name:var(--font-geist-sans)]">
      <JsonParser />
    </div>
  );
}
